{
  "name": "flight-monitor",
  "version": "1.0.0",
  "description": "Flight pickup calculator app",
  "scripts": {
    "start": "./start.sh",
    "build": "cd frontend && npm install && npm run build",
    "postinstall": "cd frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
